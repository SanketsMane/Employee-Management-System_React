# Production Environment Configuration for Formonex Deployment
# Copy this file to .env.prod and fill in your actual values

# Application Version
VERSION=v4.0

# MongoDB Configuration (REQUIRED)
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/employee-management-system?retryWrites=true&w=majority

# JWT Configuration (REQUIRED)
JWT_SECRET=your_super_secure_jwt_secret_key_for_production_at_least_64_characters_long
JWT_EXPIRE=7d

# Email Configuration (REQUIRED)
EMAIL_ENABLED=true
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=your-email@gmail.com

# Production URLs for Formonex Domain
FRONTEND_URL=http://ems.formonex.in
VITE_API_BASE_URL=http://api.ems.formonex.in/api
VITE_WEBSOCKET_URL=ws://api.ems.formonex.in
VITE_APP_NAME=Employee Management System - Formonex

# Cloudinary Configuration (REQUIRED)
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Security Configuration
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100
NODE_ENV=production

# Server Configuration
PORT=5000

# Docker Volume Paths
UPLOADS_PATH=/data/uploads
LOGS_PATH=/data/logs

# Additional Production Settings
MAX_FILE_SIZE=10mb
SESSION_TIMEOUT=7d
API_TIMEOUT=10000

# Environment specific notes:
# - DevOps will configure backend IP/domain
# - Frontend will be served from: http://ems.formonex.in
# - Backend API will be accessible via configured IP/domain
# - WebSocket connections will use same backend domain